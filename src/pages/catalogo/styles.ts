import styled from "styled-components"

export const box = styled.div`
display: flex;
flex: 0 0 auto;
flex-direction: column;
align-items: flex-start;
margin: 0 auto;
width: ${(45*2/3 + 7)*4}rem;
height: fit-content;
`